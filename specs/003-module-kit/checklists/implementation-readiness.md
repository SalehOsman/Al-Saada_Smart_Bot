# Implementation Readiness Checklist: 003-module-kit

**Purpose**: Validate that all requirements for the Module Kit (Layer 2) are complete, clear, consistent, and measurable before implementation begins
**Created**: 2026-03-02
**Feature**: `specs/003-module-kit/spec.md`
**Depth**: Standard | **Audience**: Reviewer (Technical Advisor) | **Focus**: Architecture, Data Flow, i18n, RBAC, Edge Cases

## Requirement Completeness

- [ ] CHK001 Are the exact files and folder structure generated by `module:create` fully enumerated in the requirements? [Completeness, Spec §FR-011]
- [ ] CHK002 Are all granular permission actions (`create`, `view`, `edit`, `delete`) documented with usage context for each action type? [Completeness, Spec §FR-002]
- [ ] CHK003 Are the specific PII field names to be masked (`phone`, `nationalId`, `taxId`) explicitly listed as an exhaustive list, or is the detection rule open-ended? [Completeness, Spec §FR-006]
- [ ] CHK004 Is the behavior for `save()` on update/delete operations fully specified — does the audit action change to `MODULE_UPDATE`/`MODULE_DELETE` accordingly? [Completeness, Spec §FR-006]
- [ ] CHK005 Are requirements defined for how `ModuleLoader` handles duplicate slugs across different module folders? [Completeness, Gap]
- [ ] CHK006 Are the Prisma schema migration steps from single-file to multi-file specified with backward compatibility requirements? [Completeness, Gap]

## Requirement Clarity

- [ ] CHK007 Is the `sectionSlug` → `AdminScope.scopeId` direct mapping precisely defined — does `scopeId` store the section slug string or a section database ID? [Clarity, Spec §Clarification Q1]
- [ ] CHK008 Is "under 5 seconds" for ModuleLoader startup defined with a specific measurement method — cold start only, or including i18n locale merging? [Clarity, Spec §QA-001]
- [ ] CHK009 Is the PII masking format precisely specified — is `+20*******12` (show country code + last 2) the exact format, or configurable? [Clarity, Spec §FR-006 Clarification]
- [ ] CHK010 Is "sliding TTL" behavior defined for when the TTL reset occurs — on every Redis write, or only on user message input? [Clarity, Spec §FR-008]
- [ ] CHK011 Is "Cancel & Notify" for maxRetries clearly specified — does "draft cleared" mean the entire draft or only the current field? [Clarity, Spec §Clarification Q2]

## Requirement Consistency

- [ ] CHK012 Are the `ModuleDefinition` fields consistent between contracts/module-definitions.md and data-model.md? (data-model.md may still show `name: { ar, en }` and `permissions: Role[]`) [Consistency, data-model.md vs contracts]
- [ ] CHK013 Is the AuditAction enum consistently named across spec.md, tasks.md, and quickstart.md? (`MODULE_CREATE` vs `MODULE_REGISTER`) [Consistency, Tasks §T035 vs quickstart.md]
- [ ] CHK014 Are command interrupt behaviors consistent — does `/cancel` in `validate()` (spec edge case) and draft middleware (T011) both preserve the draft? [Consistency, Spec §Edge Cases vs T011]
- [ ] CHK015 Is the conversation file naming convention consistent — does plan.md, quickstart.md, and contracts all use `add.conversation.ts` (not `conversations/index.ts`)? [Consistency, plan.md vs quickstart.md]

## Acceptance Criteria Quality

- [ ] CHK016 Is SC-001 ("under 1 minute") measurable — does it include interactive prompt time or only file generation time? [Measurability, Spec §SC-001]
- [ ] CHK017 Is SC-003 ("0% unauthorized access rate") testable — are specific test scenarios defined for each role-permission combination? [Measurability, Spec §SC-003]
- [ ] CHK018 Is SC-004 ("measurable decrease in abandoned flows") quantified with a baseline and target metric? [Measurability, Spec §SC-004]
- [ ] CHK019 Is QA-002 ("100% of successful data submissions") verifiable — is there a mechanism to count and compare save() calls to AuditLog entries? [Measurability, Spec §QA-002]

## Scenario Coverage

- [ ] CHK020 Are requirements defined for what happens when a module's `sectionSlug` references a non-existent section? [Coverage, Gap]
- [ ] CHK021 Are requirements defined for how the menu displays when a user has `view` permission but NOT `create` — is the "add" button hidden or shown-but-disabled? [Coverage, Spec §FR-002]
- [ ] CHK022 Are requirements specified for the notification message format — what data fields are included, is it a summary or full detail? [Coverage, Spec §FR-007]
- [ ] CHK023 Are requirements for `module:list` output format detailed — table columns, error display format, module count? [Coverage, Spec §FR-013]
- [ ] CHK024 Are requirements defined for what `/help` shows during a module conversation — generic help or step-specific context? [Coverage, Spec §Clarification: command interrupts]

## Edge Case Coverage

- [ ] CHK025 Is the behavior specified when a SUPER_ADMIN is the only admin and their permissions are checked via `save()` notifications? [Edge Case, Spec §FR-007]
- [ ] CHK026 Are requirements defined for what happens when Redis is unavailable — does `validate()` still work without draft saving? [Edge Case, Gap]
- [ ] CHK027 Is the behavior specified for an empty `modules/` directory on startup — does ModuleLoader log a warning or silently skip? [Edge Case, Gap]
- [ ] CHK028 Are requirements defined for `confirm()` with zero editable fields — is the edit UI hidden entirely? [Edge Case, Spec §FR-004]
- [ ] CHK029 Is the behavior specified when `prisma generate` fails during `module:create` — does the CLI rollback the created folder? [Edge Case, Spec §FR-011]
- [ ] CHK030 Are requirements for draft expiration notifications defined — is the user notified when their draft expires, or silent deletion? [Edge Case, Gap]

## Non-Functional Requirements

- [ ] CHK031 Are logging requirements specified for ModuleLoader — log level, log format, structured fields? [Non-Functional, Gap]
- [ ] CHK032 Are memory/resource requirements defined for storing module configs in-memory at startup? [Non-Functional, Gap]
- [ ] CHK033 Are concurrent draft access requirements fully specified — does LWW apply to Redis draft writes from multiple devices? [Non-Functional, Spec §Edge Cases]

## Dependencies & Assumptions

- [ ] CHK034 Is the assumption that Layer 1 `notificationService` and `auditService` exist and are ready explicitly documented? [Assumption]
- [ ] CHK035 Is the dependency on `AdminScope` table structure documented — specifically which fields are used for the direct mapping? [Dependency, Spec §Clarification Q1]
- [ ] CHK036 Is the dependency on Prisma 6.7.0's `prismaSchemaFolder` GA status validated and version-pinned? [Dependency, plan.md]
- [ ] CHK037 Is the assumption that `@grammyjs/conversations` supports state serialization/deserialization for draft recovery validated? [Assumption, research.md Section 2]

## Notes

- Check items off as completed: `[x]`
- Items marked `[Gap]` indicate missing requirements that should be addressed before or during implementation
- Items marked `[Consistency]` should be verified across all referenced files
- Total: 37 checklist items across 8 categories

# M-03: نظام المسودات (Draft System)

> **الملف:** `packages/core/src/bot/middleware/draft.ts`
> **الحالة:** ✅ مُنفذ

## شجرة التدفق

```mermaid
flowchart TD
    A["المستخدم يبدأ إدخال بيانات في موديول"] --> B["Draft Middleware يعمل تلقائياً"]
    B --> C["كل إدخال يُحفظ في Redis\nكـ draft:{telegramId}:{moduleSlug}"]

    D["المستخدم يلغي بـ /cancel\nأو يخرج من البوت"] --> E["المسودة تبقى في Redis"]

    F["المستخدم يعود لنفس الموديول"] --> G{"هل توجد مسودة محفوظة؟"}
    G -->|نعم| H["📋 عرض: 'لديك مسودة محفوظة'\nهل تريد إكمالها؟"]
    H --> H1{"اختيار المستخدم؟"}
    H1 -->|إكمال| I["استعادة البيانات من Redis\n+ متابعة من آخر خطوة"]
    H1 -->|بدء جديد| J["حذف المسودة\n+ بدء من الصفر"]
    G -->|لا| K["بدء إدخال جديد"]

    L["المستخدم يكمل الحفظ بنجاح\nsave()"] --> M["حذف المسودة من Redis تلقائياً"]

    style I fill:#2980b9,color:#fff
    style M fill:#27ae60,color:#fff
    style E fill:#f39c12,color:#fff
```

## تفاصيل تقنية

| العنصر | التفاصيل |
|--------|---------|
| مفتاح Redis | `draft:{telegramId}:{moduleSlug}` |
| البيانات المحفوظة | `session` كاملة (تشمل `currentModule`, `currentSection`, وبيانات الحقول) |
| موعد الحذف | عند نجاح `save()` أو بدء إدخال جديد |
| TTL | لا يوجد حالياً (BL-003: مخطط إضافة 15 دقيقة timeout) |

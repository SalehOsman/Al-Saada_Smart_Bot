# C-09: إدارة الأقسام (Section Management)

> **الحالة:** ⏳ غير مُنفذ (T035–T040)
> **متاح لـ:** SUPER_ADMIN

## شجرة التدفق المخططة

```mermaid
flowchart TD
    A["Super Admin يضغط 📁 الأقسام"] --> B["عرض قائمة الأقسام النشطة"]
    B --> C{"الإجراء؟"}

    C -->|"إنشاء قسم جديد"| D["إدخال اسم القسم\n(2-50 حرف)"]
    D --> D1["إدخال أيقونة القسم\n(emoji واحد)"]
    D1 --> D2["✅ إنشاء القسم في DB"]

    C -->|"تعديل قسم"| E["تعديل الاسم أو الأيقونة"]
    E --> E1["✅ تحديث القسم"]

    C -->|"حذف قسم"| F{"القسم يحتوي موديولات نشطة؟"}
    F -->|نعم| F1["⛔ رفض الحذف\n(errors-section-has-active-modules)"]
    F -->|لا| F2["✅ حذف القسم"]

    C -->|"تفعيل/تعطيل"| G["قلب حالة isActive"]

    C -->|"اختيار قسم"| H{"يحتوي موديولات؟"}
    H -->|نعم| H1["عرض قائمة الموديولات"]
    H -->|لا| H2["📩 لا يوجد موديولات بعد\n(section-empty-modules)"]

    style D2 fill:#27ae60,color:#fff
    style F1 fill:#e74c3c,color:#fff
    style F2 fill:#e74c3c,color:#fff
```

## القواعد المحددة في المواصفات (FR-018)

| القاعدة | التفاصيل |
|---------|---------|
| اسم القسم | 2–50 حرف (Zod validation) |
| أيقونة القسم | emoji واحد فقط — regex: `/^\p{Emoji}$/u` |
| الحذف | مسموح فقط إذا 0 موديولات نشطة |
| التعطيل | يُخفي القسم من القوائم لكن لا يحذفه |

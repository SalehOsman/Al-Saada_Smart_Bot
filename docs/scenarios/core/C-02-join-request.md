# C-02: طلب الانضمام (Join Request)

> **الملف المصدري:** `packages/core/src/bot/conversations/join.ts`
> **الحالة:** ✅ مُنفذ

## شجرة التدفق

```mermaid
flowchart TD
    A["بدء محادثة الانضمام"] --> B["📩 رسالة ترحيب (join-welcome)"]
    B --> C["الخطوة 1: إدخال الاسم الكامل بالعربية"]
    C --> C1{"اسم عربي صحيح؟"}
    C1 -->|لا| C2["⚠️ خطأ تحقق — إعادة الطلب"]
    C2 --> C
    C1 -->|إلغاء /cancel| X["❌ إلغاء المحادثة"]
    C1 -->|نعم| D["الخطوة 2: اللقب (اختياري)"]

    D --> D1{"أدخل لقب أم تخطى؟"}
    D1 -->|تخطى| D2["🔄 توليد لقب تلقائي من الاسم"]
    D1 -->|أدخل لقب| D3["✅ حفظ اللقب"]
    D1 -->|إلغاء| X
    D2 --> E
    D3 --> E

    E["الخطوة 3: رقم الهاتف المصري"] --> E1{"رقم صحيح (010/011/012/015)؟"}
    E1 -->|لا| E2["⚠️ خطأ — إعادة الطلب"]
    E2 --> E
    E1 -->|إلغاء| X
    E1 -->|نعم| F

    F["الخطوة 4: الرقم القومي (14 رقم)"] --> F1{"رقم قومي صالح؟"}
    F1 -->|لا| F2["⚠️ خطأ — إعادة الطلب"]
    F2 --> F
    F1 -->|إلغاء| X
    F1 -->|نعم| G

    G["الخطوة 5: شاشة التأكيد\n(الاسم + اللقب + الهاتف + الرقم القومي + تاريخ الميلاد + الجنس)"]
    G --> G1{"تأكيد؟"}
    G1 -->|لا| X
    G1 -->|نعم| H{"createOrBootstrap()"}

    H -->|"type == bootstrap\n(أول مستخدم + INITIAL_SUPER_ADMIN_ID)"| I["🎉 أهلاً Super Admin!\n(welcome-super-admin-new)"]
    H -->|"type == request\n(مستخدم عادي)"| J["📩 تم استلام طلبك\n(join-request-received)\n+ إشعار لكل الـ Admins"]

    X --> X1["حذف كل رسائل المحادثة\n+ عرض زر إعادة المحاولة"]

    style I fill:#27ae60,color:#fff
    style J fill:#2980b9,color:#fff
    style X fill:#e74c3c,color:#fff
```

## جدول الخطوات

| الخطوة | المطلوب من المستخدم | تحقق (Validation) | رسالة الخطأ |
|--------|-------------------|-------------------|------------|
| 1. الاسم | اسم عربي كامل | `askForArabicName` — أحرف عربية فقط | خطأ تحقق + إعادة |
| 2. اللقب | نص حر أو "تخطى" | اختياري — زر تخطى متاح | — |
| 3. الهاتف | رقم مصري | `askForPhone` — يبدأ بـ 010/011/012/015 | خطأ تحقق + إعادة |
| 4. الرقم القومي | 14 رقم | `askForNationalId` — استخراج تاريخ الميلاد والجنس | خطأ تحقق + إعادة |
| 5. التأكيد | ضغط زر ✅ أو ❌ | — | — |

## حالة Bootstrap

إذا كانت قاعدة البيانات فارغة (0 مستخدمين) و `telegramId` يطابق `INITIAL_SUPER_ADMIN_ID`:
- يتم إنشاء المستخدم فوراً بدور `SUPER_ADMIN`
- لا يحتاج موافقة أحد
- يظهر له `welcome-super-admin-new`

## الحالات الاستثنائية

- **إلغاء في أي خطوة**: حذف كل الرسائل + عرض زر "إعادة المحاولة" (`button-submit-join-request`)
- **خطأ في الخادم**: حذف الرسائل + `throw error` (يُلتقط بواسطة error middleware)
- **telegramId = 0**: رسالة `error-invalid-telegram-id` ثم خروج

# C-04: القائمة الرئيسية والتنقل (Menu Navigation)

> **الملف المصدري:** `packages/core/src/bot/handlers/menu.ts`
> **الحالة:** ✅ مُنفذ

## شجرة التدفق

```mermaid
flowchart TD
    A["menuHandler يُستدعى"] --> B{"المستخدم موجود وفعّال؟"}
    B -->|"لا (null أو !isActive)"| C["⛔ رسالة: حسابك غير مفعل\n(user-inactive)"]
    B -->|نعم| D["📝 تسجيل MENU_ACCESS في Audit"]
    D --> E["🔍 جلب الموديولات المصرح بها\ngetAuthorizedModules()"]
    E --> F{"ما هو دور المستخدم؟"}

    F -->|SUPER_ADMIN| G["📋 القائمة الكاملة:\n• الأقسام + المستخدمون\n• الصيانة + التدقيق\n• الموديولات + الإشعارات\n+ كل الموديولات المحمّلة"]

    F -->|ADMIN| H["📋 قائمة محدودة:\n• الأقسام + المستخدمون\n• الصيانة + التدقيق\n+ الموديولات ضمن نطاقه فقط"]

    F -->|EMPLOYEE| I["📋 قائمة أساسية:\n• الأقسام\n+ الموديولات المصرح بها في permissions.view"]

    style G fill:#8e44ad,color:#fff
    style H fill:#2980b9,color:#fff
    style I fill:#27ae60,color:#fff
    style C fill:#e74c3c,color:#fff
```

## أزرار القائمة حسب الدور

### SUPER_ADMIN
```
┌──────────────┬──────────────┐
│  📁 الأقسام  │  👥 المستخدمون │
├──────────────┼──────────────┤
│  🔧 الصيانة  │  📋 التدقيق   │
├──────────────┼──────────────┤
│  📦 الموديولات │  🔔 الإشعارات │
├──────────────┴──────────────┤
│     [كل الموديولات المحمّلة]   │
└─────────────────────────────┘
```

### ADMIN
```
┌──────────────┬──────────────┐
│  📁 الأقسام  │  👥 المستخدمون │
├──────────────┼──────────────┤
│  🔧 الصيانة  │  📋 التدقيق   │
├──────────────┴──────────────┤
│  [الموديولات ضمن AdminScope]  │
└─────────────────────────────┘
```

### EMPLOYEE
```
┌─────────────────────────────┐
│         📁 الأقسام           │
├─────────────────────────────┤
│  [الموديولات التي يملك view]  │
└─────────────────────────────┘
```

## منطق تصفية الموديولات

```mermaid
flowchart TD
    A["allModules = moduleLoader.getLoadedModules()"] --> B{"الدور؟"}
    B -->|SUPER_ADMIN| C["يرى كل الموديولات"]
    B -->|ADMIN| D["تصفية:\n1. permissions.view يشمل ADMIN\n2. sectionSlug ضمن AdminScope"]
    B -->|EMPLOYEE| E["تصفية:\npermissions.view يشمل EMPLOYEE"]
    B -->|VISITOR| F["لا يرى أي موديول"]
```

## مفاتيح i18n المستخدمة

| المفتاح | الاستخدام |
|--------|----------|
| `menu-super_admin` | رسالة ترحيب القائمة لـ Super Admin |
| `menu-admin` | رسالة ترحيب القائمة لـ Admin |
| `menu-employee` | رسالة ترحيب القائمة لـ Employee |
| `button-sections` | زر الأقسام |
| `button-users` | زر المستخدمون |
| `button-maintenance` | زر الصيانة |
| `button-audit` | زر التدقيق |
| `button-modules` | زر الموديولات |
| `button-notifications` | زر الإشعارات |
